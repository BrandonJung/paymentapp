const { default: CardContainer } = require('@/app/components/cardContainer');

const ViewJobsPage = () => {
  return <CardContainer title={'View Jobs'}></CardContainer>;
};

export default ViewJobsPage;
